{
  "name": "@dev-agents/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dev-agents/shared": "workspace:*",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@dev-agents/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
