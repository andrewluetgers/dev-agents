{
  "mcpServers": {
    "agent": {
      "command": "bun",
      "args": ["image/channel/agent-channel.ts"],
      "env": {
        "ORCHESTRATOR_HOME": "/Users/luetgers.andrew/dev-agents/orchestrator",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
